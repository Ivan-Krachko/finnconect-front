/** Ancho / alto del recorte visual del óvalo (debe coincidir con el layout de máscara). */
export const FACE_OVAL_W = 280;
export const FACE_OVAL_H = 360;

/** Superficie clara del encuadre facial (referencia: máscara blanca alrededor del óvalo). */
export const FACE_UI_SURFACE = "#FFFFFF";
/** Acento principal (botón, borde del óvalo en estado neutro). */
export const FACE_PRIMARY = "#1FA774";
